---
phase: 01-dashboard-redesign
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/skeletons/ReportsSkeleton.tsx
  - src/components/skeletons/AuditDetailSkeleton.tsx
  - src/pages/Reports.tsx
  - src/pages/Reports.test.tsx
  - src/pages/AuditDetail.tsx
  - src/pages/AuditDetail.test.tsx
autonomous: true
requirements: [PLSH-01]

must_haves:
  truths:
    - "User sees shimmer skeleton placeholders while Reports page data is loading"
    - "User sees shimmer skeleton placeholders while AuditDetail page data is loading"
    - "Skeletons match the structural layout of each page (not a generic spinner)"
  artifacts:
    - path: "src/components/skeletons/ReportsSkeleton.tsx"
      provides: "Skeleton loading state for Reports page"
      exports: ["ReportsSkeleton"]
    - path: "src/components/skeletons/AuditDetailSkeleton.tsx"
      provides: "Skeleton loading state for AuditDetail page"
      exports: ["AuditDetailSkeleton"]
    - path: "src/pages/Reports.tsx"
      provides: "Reports page with skeleton loading instead of spinner"
    - path: "src/pages/AuditDetail.tsx"
      provides: "AuditDetail page with skeleton loading instead of spinner"
  key_links:
    - from: "src/pages/Reports.tsx"
      to: "src/components/skeletons/ReportsSkeleton.tsx"
      via: "import and conditional render on loading"
      pattern: "import.*ReportsSkeleton.*from.*skeletons/ReportsSkeleton"
    - from: "src/pages/AuditDetail.tsx"
      to: "src/components/skeletons/AuditDetailSkeleton.tsx"
      via: "import and conditional render on loading"
      pattern: "import.*AuditDetailSkeleton.*from.*skeletons/AuditDetailSkeleton"
---

<objective>
Replace spinner loading states on Reports and AuditDetail pages with shimmer skeleton placeholders that match each page's structural layout.

Purpose: Consistent loading experience across all data-fetching pages (PLSH-01), reducing perceived load time and preventing layout shift.
Output: Two skeleton components (ReportsSkeleton, AuditDetailSkeleton), updated Reports.tsx and AuditDetail.tsx with skeleton loading, updated tests.
</objective>

<execution_context>
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dashboard-redesign/01-CONTEXT.md
@.planning/phases/01-dashboard-redesign/01-UI-SPEC.md

<interfaces>
<!-- Key types and patterns from Plan 01 -->

From src/components/ui/skeleton.tsx:
```typescript
function Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>)
// Uses: cn("animate-pulse rounded-md bg-primary/10", className)
```

From src/components/dashboard/DashboardSkeleton.tsx (pattern reference):
```typescript
// Uses Skeleton component with structural layout matching the loaded page
// Page container → heading skeleton → grid/card skeletons → row skeletons
export function DashboardSkeleton()
```

From src/hooks/useAudits.ts:
```typescript
export function useAudits(): { audits: Audit[], loading: boolean, error: string | null, refetch: () => Promise<void> }
```

From src/hooks/useAudit.ts:
```typescript
export function useAudit(id: string | undefined): { audit: Audit | null, loading: boolean }
```

From src/pages/Reports.tsx (current loading state):
```typescript
if (loading) {
  return (
    <div className="flex items-center justify-center h-full">
      <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-primary" />
    </div>
  )
}
```

From src/pages/AuditDetail.tsx (current loading state):
```typescript
if (loading) {
  return (
    <div className="flex items-center justify-center h-full">
      <div className="animate-spin rounded-full h-10 w-10 border-b-2 border-[#4F46E5]" />
    </div>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton components and wire into Reports and AuditDetail pages</name>
  <files>src/components/skeletons/ReportsSkeleton.tsx, src/components/skeletons/AuditDetailSkeleton.tsx, src/pages/Reports.tsx, src/pages/AuditDetail.tsx</files>
  <read_first>
    - src/pages/Reports.tsx (current layout to mirror in skeleton)
    - src/pages/AuditDetail.tsx (current layout to mirror in skeleton)
    - src/components/ui/skeleton.tsx (Skeleton component API)
    - src/components/dashboard/DashboardSkeleton.tsx (pattern reference from Plan 01)
    - src/lib/utils.ts (cn helper)
  </read_first>
  <action>
1. Create `src/components/skeletons/ReportsSkeleton.tsx`:
   Import `Skeleton` from `@/components/ui/skeleton`.
   Mirror the Reports.tsx loaded layout structure:
   ```
   <div className="p-6 lg:p-8">
     {/* Header skeleton */}
     <div className="mb-8 flex items-center justify-between">
       <div>
         <Skeleton className="h-7 w-40 mb-2" />
         <Skeleton className="h-4 w-64" />
       </div>
       <Skeleton className="h-9 w-28 rounded-md" />
     </div>

     {/* Filter buttons skeleton */}
     <div className="flex gap-2 mb-6">
       {Array.from({ length: 4 }).map((_, i) => (
         <Skeleton key={i} className="h-8 w-24 rounded-md" />
       ))}
     </div>

     {/* Audit cards skeleton */}
     <div className="space-y-3">
       {Array.from({ length: 3 }).map((_, i) => (
         <div key={i} className="rounded-xl border border-border bg-card p-5 shadow-sm">
           <div className="flex items-center justify-between">
             <div className="flex items-center gap-4 flex-1">
               <Skeleton className="h-12 w-12 rounded-lg" />
               <div className="flex-1">
                 <Skeleton className="h-5 w-48 mb-2" />
                 <div className="flex gap-4">
                   <Skeleton className="h-4 w-20" />
                   <Skeleton className="h-4 w-16" />
                   <Skeleton className="h-4 w-24" />
                 </div>
               </div>
             </div>
             <div className="flex items-center gap-2">
               <Skeleton className="h-6 w-20 rounded-full" />
               <Skeleton className="h-8 w-16 rounded-md" />
               <Skeleton className="h-8 w-8 rounded-md" />
             </div>
           </div>
         </div>
       ))}
     </div>
   </div>
   ```

2. Create `src/components/skeletons/AuditDetailSkeleton.tsx`:
   Import `Skeleton` from `@/components/ui/skeleton`.
   Mirror the AuditDetail.tsx loaded layout structure (header bar + 3-column overview + issues panel):
   ```
   <div className="flex flex-col h-full overflow-hidden">
     {/* Header skeleton */}
     <div className="flex items-center justify-between px-6 py-3 border-b border-border bg-card">
       <Skeleton className="h-8 w-36 rounded-md" />
       <Skeleton className="h-8 w-32 rounded-md" />
     </div>

     {/* Overview section skeleton */}
     <div className="px-6 py-5 border-b border-border bg-card shrink-0">
       <div className="flex items-center gap-2 mb-5">
         <Skeleton className="h-6 w-24" />
         <Skeleton className="h-4 w-48" />
       </div>
       <div className="grid grid-cols-3 gap-4">
         {Array.from({ length: 3 }).map((_, i) => (
           <div key={i} className="rounded-xl border border-border p-4">
             <Skeleton className="h-4 w-20 mb-3" />
             <Skeleton className="h-16 w-16 mx-auto rounded-full mb-2" />
             <Skeleton className="h-3 w-24 mx-auto" />
           </div>
         ))}
       </div>
     </div>

     {/* Issues panel skeleton */}
     <div className="flex flex-1 overflow-hidden">
       {/* Left: Issue list */}
       <div className="w-[280px] shrink-0 border-r border-border bg-card p-4">
         <div className="flex gap-2 mb-4">
           {Array.from({ length: 4 }).map((_, i) => (
             <Skeleton key={i} className="h-8 w-16 rounded-md" />
           ))}
         </div>
         {Array.from({ length: 4 }).map((_, i) => (
           <div key={i} className="mb-3">
             <Skeleton className="h-5 w-full mb-1" />
             <Skeleton className="h-3 w-3/4" />
           </div>
         ))}
       </div>
       {/* Middle: Details */}
       <div className="flex-1 bg-card p-5">
         <Skeleton className="h-6 w-20 mb-4" />
         <Skeleton className="h-4 w-full mb-2" />
         <Skeleton className="h-4 w-3/4 mb-4" />
         <Skeleton className="h-24 w-full rounded-lg mb-4" />
         <Skeleton className="h-16 w-full rounded-lg" />
       </div>
       {/* Right: How to Fix */}
       <div className="w-[340px] shrink-0 bg-card p-5">
         <Skeleton className="h-6 w-28 mb-4" />
         <Skeleton className="h-4 w-full mb-2" />
         <Skeleton className="h-4 w-full mb-2" />
         <Skeleton className="h-4 w-3/4 mb-2" />
         <Skeleton className="h-4 w-1/2" />
       </div>
     </div>
   </div>
   ```

3. Update `src/pages/Reports.tsx` loading state:
   - Add import: `import { ReportsSkeleton } from '@/components/skeletons/ReportsSkeleton'`
   - Replace the `if (loading)` block (lines 51-57) with:
     ```
     if (loading) {
       return <ReportsSkeleton />
     }
     ```
   - Also replace `getScoreColor` and `getScoreBg` local functions (lines 14-24) with imports from `@/lib/format`:
     `import { getScoreColor, getScoreBg } from '@/lib/format'`
     Then delete the local `function getScoreColor` and `function getScoreBg` definitions.
   - No other changes to Reports.tsx.

4. Update `src/pages/AuditDetail.tsx` loading state:
   - Add import: `import { AuditDetailSkeleton } from '@/components/skeletons/AuditDetailSkeleton'`
   - Replace the `if (loading)` block (lines 30-36) with:
     ```
     if (loading) {
       return <AuditDetailSkeleton />
     }
     ```
   - No other changes to AuditDetail.tsx.
  </action>
  <verify>
    <automated>cd /Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility\ Audit\ generator && npx vitest run src/pages/Reports.test.tsx src/pages/AuditDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/skeletons/ReportsSkeleton.tsx exists and exports ReportsSkeleton
    - File src/components/skeletons/AuditDetailSkeleton.tsx exists and exports AuditDetailSkeleton
    - ReportsSkeleton.tsx imports Skeleton from "@/components/ui/skeleton"
    - AuditDetailSkeleton.tsx imports Skeleton from "@/components/ui/skeleton"
    - Reports.tsx contains `import { ReportsSkeleton }` from skeletons
    - Reports.tsx does NOT contain "animate-spin" (spinner removed)
    - Reports.tsx loading block returns `<ReportsSkeleton />`
    - Reports.tsx imports getScoreColor and getScoreBg from "@/lib/format" (not local definitions)
    - Reports.tsx does NOT contain `function getScoreColor` local definition
    - AuditDetail.tsx contains `import { AuditDetailSkeleton }` from skeletons
    - AuditDetail.tsx does NOT contain "animate-spin" (spinner removed)
    - AuditDetail.tsx loading block returns `<AuditDetailSkeleton />`
  </acceptance_criteria>
  <done>Reports.tsx and AuditDetail.tsx loading states replaced with structural skeleton components. Score color utilities consolidated to shared format.ts imports.</done>
</task>

<task type="auto">
  <name>Task 2: Update Reports and AuditDetail tests for skeleton loading</name>
  <files>src/pages/Reports.test.tsx, src/pages/AuditDetail.test.tsx</files>
  <read_first>
    - src/pages/Reports.test.tsx (current tests -- line 42-46 tests spinner)
    - src/pages/AuditDetail.test.tsx (current tests -- line 35-39 tests spinner)
    - src/pages/Reports.tsx (updated in Task 1 -- now uses ReportsSkeleton)
    - src/pages/AuditDetail.tsx (updated in Task 1 -- now uses AuditDetailSkeleton)
    - src/components/skeletons/ReportsSkeleton.tsx (to know what it renders)
    - src/components/skeletons/AuditDetailSkeleton.tsx (to know what it renders)
  </read_first>
  <action>
1. Update `src/pages/Reports.test.tsx`:
   - Find the test `shows loading spinner while fetching` (line 42-46).
   - Change test name to `shows skeleton loading while fetching`.
   - Replace assertion from `expect(document.querySelector('.animate-spin')).toBeInTheDocument()` to `expect(document.querySelector('.animate-pulse')).toBeInTheDocument()` (skeleton uses animate-pulse).
   - Add negative assertion: `expect(document.querySelector('.animate-spin')).not.toBeInTheDocument()` to confirm spinner is gone.
   - All other tests in Reports.test.tsx remain unchanged -- they test the loaded state which has not changed.

2. Update `src/pages/AuditDetail.test.tsx`:
   - Find the test `shows loading state` (line 35-39).
   - Change test name to `shows skeleton loading state`.
   - Replace assertion from `expect(screen.getByText(/loading audit/i)).toBeInTheDocument()` to `expect(document.querySelector('.animate-pulse')).toBeInTheDocument()`.
   - Add negative assertion: `expect(document.querySelector('.animate-spin')).not.toBeInTheDocument()`.
   - All other tests in AuditDetail.test.tsx remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility\ Audit\ generator && npx vitest run src/pages/Reports.test.tsx src/pages/AuditDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Reports.test.tsx contains test named "shows skeleton loading while fetching" (not "shows loading spinner while fetching")
    - Reports.test.tsx contains assertion for "animate-pulse" in loading test
    - Reports.test.tsx does NOT assert "animate-spin" as present (may assert it's absent)
    - AuditDetail.test.tsx contains test named "shows skeleton loading state" (not "shows loading state")
    - AuditDetail.test.tsx contains assertion for "animate-pulse" in loading test
    - AuditDetail.test.tsx does NOT contain "loading audit" text assertion
    - All existing non-loading tests in both files remain unchanged and passing
    - npx vitest run src/pages/Reports.test.tsx src/pages/AuditDetail.test.tsx exits 0
  </acceptance_criteria>
  <done>Reports and AuditDetail loading tests updated from spinner to skeleton assertions. All tests passing. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/pages/Reports.test.tsx` -- Reports tests pass with skeleton loading assertion
2. `npx vitest run src/pages/AuditDetail.test.tsx` -- AuditDetail tests pass with skeleton loading assertion
3. `npx vitest run` -- full test suite green (no regressions from Plan 01 or Plan 02)
4. Manual: Load `/reports` while data fetches -- shimmer skeleton visible (not spinner)
5. Manual: Load `/audits/:id` while data fetches -- shimmer skeleton visible (not spinner)
</verification>

<success_criteria>
- Reports page shows structural skeleton during loading (not spinner)
- AuditDetail page shows structural skeleton during loading (not spinner)
- Score color utilities in Reports.tsx use shared imports from format.ts (no local duplicates)
- All 3 pages (Dashboard, Reports, AuditDetail) use skeleton loading pattern consistently
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-redesign/01-02-SUMMARY.md`
</output>
