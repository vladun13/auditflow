---
phase: 02-scan-results-redesign
plan: 01
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - src/pages/NewScan.tsx
  - src/components/new-scan/ChecksList.tsx
  - src/components/new-scan/ScanProgress.tsx
  - src/components/new-scan/constants.ts
  - src/components/Hero.tsx
  - src/pages/Login.tsx
  - src/pages/SignUp.tsx
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-04, AUTH-01]

must_haves:
  truths:
    - "User sees 2-column layout with URL form on left and WCAG checklist on right"
    - "User sees animated scan progress steps in right panel during active scan"
    - "User with 0 credits sees disabled scan button and upgrade banner"
    - "User's URL is pre-filled from ?url= query param"
    - "Unauthenticated user's URL is preserved through login via sessionStorage"
  artifacts:
    - path: "src/pages/NewScan.tsx"
      provides: "2-column scan page with form left, panel right"
      min_lines: 100
    - path: "src/components/new-scan/ScanProgress.tsx"
      provides: "Animated 3-step scan progress for right panel"
      exports: ["ScanProgress"]
    - path: "src/components/new-scan/ChecksList.tsx"
      provides: "WCAG principle checklist (Perceivable, Operable, Understandable, Robust)"
      exports: ["ChecksList"]
  key_links:
    - from: "src/components/Hero.tsx"
      to: "sessionStorage"
      via: "setItem('auditflow_pending_url', url)"
      pattern: "sessionStorage\\.setItem.*auditflow_pending_url"
    - from: "src/pages/Login.tsx"
      to: "/scan"
      via: "navigate after auth with sessionStorage URL"
      pattern: "sessionStorage\\.getItem.*auditflow_pending_url"
    - from: "src/pages/NewScan.tsx"
      to: "useCredits"
      via: "credits check disables scan button"
      pattern: "useCredits|credits.*===.*0|noCredits"
---

<objective>
Redesign NewScan.tsx to 2-column layout matching Figma, with WCAG checklist right panel that swaps to animated scan progress during active scan. Implement zero-credits blocking with upgrade prompt. Wire AUTH-01 sessionStorage URL preservation through Hero -> Login -> /scan.

Purpose: Users get a polished scan initiation flow and unauthenticated URL preservation.
Output: Redesigned NewScan page, ScanProgress component, updated ChecksList, AUTH-01 flow wired.
</objective>

<execution_context>
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scan-results-redesign/02-CONTEXT.md
@.planning/phases/02-scan-results-redesign/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/index.ts:
```typescript
export interface Audit {
  id: string
  user_id: string
  website_url: string
  status: 'pending' | 'scanning' | 'completed' | 'failed'
  pages_scanned: number | null
  total_violations: number | null
  critical_count: number | null
  serious_count: number | null
  moderate_count: number | null
  minor_count: number | null
  wcag_score: number | null
  wcag_level: string | null
  created_at: string
  completed_at: string | null
}
```

From src/hooks/useCredits.ts:
```typescript
export function useCredits(): { credits: number | null; loading: boolean; error: string | null; refetch: () => void }
```

From src/hooks/useAudit.ts:
```typescript
export function useAudit(id: string | undefined): { audit: Audit | null; loading: boolean; error: string | null }
```

From src/lib/api.ts:
```typescript
export const auditApi = {
  list(): Promise<Audit[]>
  get(id: string): Promise<Audit & { violations?: Violation[] }>
  create(data: { website_url: string; crawl_depth: number }): Promise<Audit>
  scan(id: string): Promise<void>
  delete(id: string): Promise<void>
  downloadPdf(id: string): Promise<Blob>
}
```

From src/lib/format.ts:
```typescript
export function getScoreColor(score: number): string  // green-600 / yellow-600 / red-600
export function getScoreBg(score: number): string      // green-50 / yellow-50 / red-50
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScanProgress component and update ChecksList</name>
  <files>src/components/new-scan/ScanProgress.tsx, src/components/new-scan/ChecksList.tsx, src/components/new-scan/constants.ts</files>
  <read_first>src/components/new-scan/ChecksList.tsx, src/components/new-scan/constants.ts, src/components/new-scan/UrlInput.tsx, src/components/new-scan/DepthSelector.tsx</read_first>
  <action>
1. **Create `src/components/new-scan/ScanProgress.tsx`:**
   - Export `function ScanProgress({ currentStep }: { currentStep: number })`
   - 3 steps: `{ id: 'crawl', label: 'Crawling pages', description: 'Discovering linked pages' }`, `{ id: 'analyze', label: 'Running axe-core', description: 'Testing WCAG compliance' }`, `{ id: 'ai', label: 'Generating AI fixes', description: 'Creating fix recommendations' }`
   - Each step shows: CheckCircle (green-500) if done, Loader2 (animate-spin, text-[#4F46E5]) if active, empty circle (border-2 border-gray-200) if pending
   - Use Framer Motion `motion.div` with `initial={{ opacity: 0, x: -10 }}` `animate={{ opacity: 1, x: 0 }}` `transition={{ delay: i * 0.15 }}` for staggered entry
   - Use `cn()` from `@/lib/utils` for conditional classes
   - Import: `{ motion } from 'framer-motion'`, `{ CheckCircle, Loader2 } from 'lucide-react'`, `{ cn } from '@/lib/utils'`

2. **Rewrite `src/components/new-scan/ChecksList.tsx`:**
   - Organize as WCAG principles: Perceivable, Operable, Understandable, Robust
   - Each principle: icon (Eye, MousePointer, Brain, Shield from lucide-react), title, 2-3 sub-items as bullet text
   - Sub-items examples: Perceivable: "Image alt text", "Color contrast ratios", "Video captions". Operable: "Keyboard navigation", "Focus indicators", "Time limits". Understandable: "Form labels", "Error messages", "Consistent navigation". Robust: "Valid HTML", "ARIA attributes", "Assistive tech compatibility"
   - Card-style layout with rounded-xl border border-gray-100 bg-white p-4

3. **Update `src/components/new-scan/constants.ts`:**
   - Add `WCAG_PRINCIPLES` array with `{ id, label, icon, items: string[] }` for each principle
   - Add `SCAN_STEPS` array with `{ id, label, description }` for the 3 scan steps
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && npx tsc --noEmit src/components/new-scan/ScanProgress.tsx src/components/new-scan/ChecksList.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ScanProgress" src/components/new-scan/ScanProgress.tsx
    - grep -q "CheckCircle" src/components/new-scan/ScanProgress.tsx
    - grep -q "Loader2" src/components/new-scan/ScanProgress.tsx
    - grep -q "AnimatePresence\|motion" src/components/new-scan/ScanProgress.tsx
    - grep -q "Perceivable" src/components/new-scan/ChecksList.tsx
    - grep -q "Operable" src/components/new-scan/ChecksList.tsx
    - grep -q "WCAG_PRINCIPLES\|SCAN_STEPS" src/components/new-scan/constants.ts
  </acceptance_criteria>
  <done>ScanProgress component renders 3 animated steps with done/active/pending states. ChecksList shows 4 WCAG principles with sub-items. Constants exported for both.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign NewScan.tsx with 2-column layout, progress panel, zero-credits block</name>
  <files>src/pages/NewScan.tsx</files>
  <read_first>src/pages/NewScan.tsx, src/components/new-scan/UrlInput.tsx, src/components/new-scan/DepthSelector.tsx, src/components/new-scan/StandardsSelect.tsx, src/hooks/useCredits.ts</read_first>
  <action>
1. **Rewrite `src/pages/NewScan.tsx` as 2-column layout:**
   - Left column: URL form (UrlInput, DepthSelector, StandardsSelect, scan button)
   - Right column: ChecksList (idle) or ScanProgress (scanning)
   - Grid: `grid lg:grid-cols-2 gap-8` with left column as card (rounded-xl border bg-white p-6) and right column as card
   - Heading at top: "New Accessibility Scan" with subtitle

2. **Right panel state switching:**
   - When `!loading`: render `<ChecksList />`
   - When `loading` (scan in progress): render `<ScanProgress currentStep={scanStep} />`
   - `scanStep` state: start at 0, increment to 1 after 5 seconds, increment to 2 after 10 seconds (simulate progress since backend doesn't report steps)
   - Use `useEffect` with `setTimeout` to advance steps while `loading` is true, clean up on unmount

3. **Zero credits block (SCAN-03):**
   - Check `credits === 0` from `useCredits()` hook
   - When zero: disable the "Start Scan" button (`disabled` prop + `opacity-50 cursor-not-allowed`)
   - Show inline banner below button: amber/yellow background, text "No credits remaining. Upgrade to continue." with `<Link to="/settings/plans">` styled as button "Upgrade"
   - Do NOT show the banner when credits > 0 or credits is null (loading)

4. **URL pre-fill (SCAN-04):**
   - Keep existing `useSearchParams().get('url')` logic for `?url=` param
   - Also check `sessionStorage.getItem('auditflow_pending_url')` on mount
   - If sessionStorage has value, set it as URL and remove from sessionStorage immediately
   - Priority: `?url=` param takes precedence over sessionStorage

5. **Preserve existing scan logic:** `auditApi.create()` then `auditApi.scan()` then `navigate(/audits/${id})`
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && npx vitest run src/pages/NewScan.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ChecksList" src/pages/NewScan.tsx
    - grep -q "ScanProgress" src/pages/NewScan.tsx
    - grep -q "useCredits" src/pages/NewScan.tsx
    - grep -q "No credits remaining" src/pages/NewScan.tsx
    - grep -q "Upgrade" src/pages/NewScan.tsx
    - grep -q "auditflow_pending_url" src/pages/NewScan.tsx
    - grep -q "sessionStorage" src/pages/NewScan.tsx
    - grep -q "lg:grid-cols-2\|lg:grid-cols" src/pages/NewScan.tsx
  </acceptance_criteria>
  <done>NewScan renders 2-column layout. Right panel shows WCAG checklist idle, animated progress during scan. Zero credits shows disabled button + upgrade banner. URL pre-filled from ?url= or sessionStorage. All NewScan tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AUTH-01 sessionStorage URL preservation through Hero and Login</name>
  <files>src/components/Hero.tsx, src/pages/Login.tsx, src/pages/SignUp.tsx</files>
  <read_first>src/components/Hero.tsx, src/pages/Login.tsx, src/pages/SignUp.tsx, src/contexts/AuthContext.tsx</read_first>
  <action>
1. **Modify `src/components/Hero.tsx`:**
   - Find the "Start Scanning" / scan CTA link/button that navigates to `/scan?url=...`
   - Before navigating, call `sessionStorage.setItem('auditflow_pending_url', url)` where `url` is the user-entered URL value
   - Keep the existing navigation to `/scan?url=...` unchanged (works for logged-in users)
   - Use constant `const SESSION_URL_KEY = 'auditflow_pending_url'`

2. **Modify `src/pages/Login.tsx`:**
   - After successful authentication (in the success handler after `signIn` resolves without error):
   - Check `const pendingUrl = sessionStorage.getItem('auditflow_pending_url')`
   - If pendingUrl exists: `sessionStorage.removeItem('auditflow_pending_url')` then `navigate('/scan?url=' + encodeURIComponent(pendingUrl))`
   - If no pendingUrl: `navigate('/dashboard')` (existing behavior)
   - Import `useNavigate` from react-router-dom if not already imported

3. **Modify `src/pages/SignUp.tsx`:**
   - Apply same sessionStorage check as Login.tsx after successful sign-up
   - Same pattern: check for pending URL, redirect to /scan or /dashboard

4. **Session key constant:** Use string literal `'auditflow_pending_url'` in all 3 files (Hero, Login, SignUp). No shared constant file needed for 3 usages.
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && npx vitest run src/pages/Login.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "auditflow_pending_url" src/components/Hero.tsx
    - grep -q "sessionStorage.setItem" src/components/Hero.tsx
    - grep -q "auditflow_pending_url" src/pages/Login.tsx
    - grep -q "sessionStorage.getItem" src/pages/Login.tsx
    - grep -q "sessionStorage.removeItem" src/pages/Login.tsx
    - grep -q "auditflow_pending_url" src/pages/SignUp.tsx
  </acceptance_criteria>
  <done>Hero stores URL to sessionStorage before navigation. Login and SignUp check sessionStorage after auth, redirect to /scan with URL if present, otherwise /dashboard. sessionStorage is cleaned up after read. Login.test.tsx AUTH-01 tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/pages/NewScan.test.tsx` -- all tests pass
- `npx vitest run src/pages/Login.test.tsx` -- AUTH-01 tests pass
- `grep -q "auditflow_pending_url" src/components/Hero.tsx src/pages/Login.tsx src/pages/NewScan.tsx` -- sessionStorage wired in all 3 files
- `grep -q "No credits remaining" src/pages/NewScan.tsx` -- zero credits banner present
</verification>

<success_criteria>
- NewScan page renders 2-column layout with form left, WCAG checklist right
- Right panel swaps to animated 3-step progress during scan
- Zero credits disables scan button and shows upgrade banner
- URL pre-filled from ?url= param or sessionStorage
- Hero stores URL to sessionStorage before navigation
- Login/SignUp redirect to /scan with preserved URL after auth
- All NewScan and Login tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-scan-results-redesign/02-01-SUMMARY.md`
</output>
