---
phase: 02-scan-results-redesign
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - src/pages/NewScan.test.tsx
  - src/pages/AuditDetail.test.tsx
  - src/pages/Login.test.tsx
  - src/components/sidebar.test.tsx
  - src/layouts/DashboardLayout.test.tsx
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-04, AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05, AUTH-01]

must_haves:
  truths:
    - "Test files define expected DOM structure for redesigned components before implementation"
    - "All test assertions match the new component structure from CONTEXT.md decisions"
    - "Pre-existing sidebar and DashboardLayout test failures are fixed"
  artifacts:
    - path: "src/pages/NewScan.test.tsx"
      provides: "Tests for 2-column layout, progress panel, credits block, URL pre-fill"
      min_lines: 80
    - path: "src/pages/AuditDetail.test.tsx"
      provides: "Tests for score ring, scanning view, violation cards, severity tabs, PDF toast"
      min_lines: 100
    - path: "src/pages/Login.test.tsx"
      provides: "Tests for AUTH-01 sessionStorage URL preservation"
      min_lines: 40
  key_links:
    - from: "src/pages/NewScan.test.tsx"
      to: "src/pages/NewScan.tsx"
      via: "render + query assertions"
      pattern: "render|screen\\.get"
    - from: "src/pages/AuditDetail.test.tsx"
      to: "src/pages/AuditDetail.tsx"
      via: "render + query assertions"
      pattern: "render|screen\\.get"
---

<objective>
Rewrite test files for NewScan, AuditDetail, and Login to match the Phase 2 redesigned DOM structure. Fix pre-existing sidebar and DashboardLayout test failures. Tests are written BEFORE implementation (Wave 0) so they define the expected behavior contract.

Purpose: Tests define the target state for implementation plans 02-01 and 02-02. All tests will initially FAIL (red) and pass after implementation.
Output: 5 updated/created test files with assertions matching Phase 2 component structure.
</objective>

<execution_context>
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scan-results-redesign/02-CONTEXT.md
@.planning/phases/02-scan-results-redesign/02-RESEARCH.md
@.planning/phases/02-scan-results-redesign/02-VALIDATION.md

<interfaces>
From src/test/helpers.tsx:
```typescript
// makeAudit() factory for creating test audit objects
// renderWithRouter() for rendering with React Router context
```

From src/types/index.ts:
```typescript
export interface Audit { id, website_url, status, wcag_score, wcag_level, ... }
export interface Violation { id, violation_type, impact, ai_explanation, ai_fix_steps, ... }
```

Test setup: Vitest + React Testing Library + happy-dom
Config: vite.config.ts test section
Run: npx vitest run {file}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NewScan.test.tsx for Phase 2 DOM structure</name>
  <files>src/pages/NewScan.test.tsx</files>
  <read_first>src/pages/NewScan.test.tsx, src/pages/NewScan.tsx, src/test/helpers.tsx, src/test/setup.ts, src/hooks/useCredits.ts</read_first>
  <action>
1. **Rewrite `src/pages/NewScan.test.tsx`** with tests matching Phase 2 redesigned structure:

2. **Mock setup:**
   - Mock `@/hooks/useCredits` returning `{ credits: 3, loading: false, error: null, refetch: vi.fn() }`
   - Mock `@/lib/api` with `auditApi.create` and `auditApi.scan` as vi.fn()
   - Mock `react-router-dom` useNavigate, useSearchParams
   - Mock `@/contexts/AuthContext` with user object

3. **Test cases for SCAN-01 (2-column layout):**
   - `it('renders URL input field')` -- screen.getByPlaceholderText or getByRole('textbox') for URL input
   - `it('renders WCAG checklist in right panel')` -- screen.getByText('Perceivable'), getByText('Operable'), getByText('Understandable'), getByText('Robust')
   - `it('renders Start Scan button')` -- screen.getByRole('button', { name: /start scan/i })

4. **Test cases for SCAN-02 (scan progress):**
   - `it('shows scan progress steps when scanning')` -- trigger scan, then check for text "Crawling pages", "Running axe-core", "Generating AI fixes"

5. **Test cases for SCAN-03 (zero credits):**
   - `it('disables scan button when credits are 0')` -- mock useCredits returning credits: 0, check button is disabled
   - `it('shows upgrade banner when credits are 0')` -- screen.getByText(/no credits remaining/i), screen.getByText(/upgrade/i)

6. **Test cases for SCAN-04 (URL pre-fill):**
   - `it('pre-fills URL from search params')` -- mock useSearchParams returning url=https://example.com, check input value
   - `it('pre-fills URL from sessionStorage')` -- set sessionStorage 'auditflow_pending_url', render, check input value, verify sessionStorage cleared

7. **Use `renderWithRouter` from test helpers.** Use `vi.mock` for all mocks. Use `screen` queries from @testing-library/react.
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && grep -c "it(" src/pages/NewScan.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Perceivable" src/pages/NewScan.test.tsx
    - grep -q "no credits remaining\|No credits remaining" src/pages/NewScan.test.tsx
    - grep -q "auditflow_pending_url" src/pages/NewScan.test.tsx
    - grep -q "Crawling pages" src/pages/NewScan.test.tsx
    - grep -q "useCredits" src/pages/NewScan.test.tsx
    - grep -q "disabled" src/pages/NewScan.test.tsx
  </acceptance_criteria>
  <done>NewScan.test.tsx has 8+ test cases covering 2-column layout, WCAG checklist, scan progress, zero credits block, URL pre-fill from params and sessionStorage. Tests will fail (red) until Plan 02-01 implements the components.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite AuditDetail.test.tsx for Phase 2 DOM structure</name>
  <files>src/pages/AuditDetail.test.tsx</files>
  <read_first>src/pages/AuditDetail.test.tsx, src/pages/AuditDetail.tsx, src/test/helpers.tsx, src/hooks/useAudit.ts</read_first>
  <action>
1. **Rewrite `src/pages/AuditDetail.test.tsx`** with tests matching Phase 2 redesigned structure:

2. **Mock setup:**
   - Mock `@/hooks/useAudit` returning various states (loading, scanning, completed, failed)
   - Create test violations array: `[{ id: '1', violation_type: 'missing-alt', impact: 'critical', ai_explanation: 'Images need alt text', ai_fix_steps: 'Add alt attribute', wcag_criterion: '1.1.1', description: 'img missing alt', page_url: 'https://example.com', affected_elements: ['<img src="logo.png">'], estimated_fix_hours: 0.5 }, { id: '2', violation_type: 'low-contrast', impact: 'serious', ... }]`
   - Mock react-router-dom useParams returning { id: '123' }

3. **Test cases for AUDIT-01 (score ring):**
   - `it('renders score ring with score value')` -- mock completed audit with wcag_score: 85, check score text "85" is rendered
   - `it('renders WCAG level label')` -- check text "WCAG AA" (or the wcag_level from audit)

4. **Test cases for AUDIT-02 (scanning state):**
   - `it('shows scanning view when status is scanning')` -- mock useAudit returning status: 'scanning', check for "Crawling pages" text
   - `it('does not show violations during scanning')` -- verify violation list is not rendered

5. **Test cases for AUDIT-03 (violation cards):**
   - `it('renders violation cards collapsed by default')` -- check violation_type text visible, "Why This Matters" NOT visible
   - `it('expands violation card on click to show AI content')` -- click the card header, then check "Why This Matters" and ai_explanation text appear
   - `it('shows How to Fix section when expanded')` -- click card, check "How to Fix" heading and ai_fix_steps text

6. **Test cases for AUDIT-04 (severity filter):**
   - `it('renders severity tabs with All selected by default')` -- check All/Critical/Serious/Moderate/Minor tab buttons exist
   - `it('filters violations when severity tab is clicked')` -- click "Critical" tab, verify only critical violations shown

7. **Test cases for AUDIT-05 (PDF toast):**
   - `it('shows coming soon toast when Download PDF is clicked')` -- find Download PDF button, click it, verify toast was called (mock sonner toast)

8. **Test cases for loading/error:**
   - `it('renders skeleton while loading')` -- mock loading: true, check for skeleton elements
   - `it('shows error state on failed audit')` -- mock audit with status: 'failed'

9. **Use `renderWithRouter`, `makeAudit` from test helpers. Use `fireEvent.click` or `userEvent.click` for interactions.**
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && grep -c "it(" src/pages/AuditDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Why This Matters" src/pages/AuditDetail.test.tsx
    - grep -q "How to Fix" src/pages/AuditDetail.test.tsx
    - grep -q "Crawling pages" src/pages/AuditDetail.test.tsx
    - grep -q "Download PDF\|Download.*PDF" src/pages/AuditDetail.test.tsx
    - grep -q "Critical\|critical" src/pages/AuditDetail.test.tsx
    - grep -q "coming soon\|PDF report generation" src/pages/AuditDetail.test.tsx
    - grep -q "useAudit" src/pages/AuditDetail.test.tsx
  </acceptance_criteria>
  <done>AuditDetail.test.tsx has 11+ test cases covering score ring, scanning state, collapsible violation cards with AI content, severity tab filtering, PDF toast, loading skeleton, and error state. Tests will fail (red) until Plan 02-02 implements the components.</done>
</task>

<task type="auto">
  <name>Task 3: Create Login.test.tsx for AUTH-01 and fix sidebar + DashboardLayout tests</name>
  <files>src/pages/Login.test.tsx, src/components/sidebar.test.tsx, src/layouts/DashboardLayout.test.tsx</files>
  <read_first>src/pages/Login.tsx, src/components/sidebar.test.tsx, src/layouts/DashboardLayout.test.tsx, src/components/sidebar.tsx, src/layouts/DashboardLayout.tsx, src/test/helpers.tsx</read_first>
  <action>
1. **Create `src/pages/Login.test.tsx`** for AUTH-01:

   Mock setup:
   - Mock `@/contexts/AuthContext` with `signIn: vi.fn()` that resolves successfully
   - Mock `react-router-dom` useNavigate
   - Mock sessionStorage with vi.spyOn(Storage.prototype, 'getItem') and vi.spyOn(Storage.prototype, 'removeItem')

   Test cases:
   - `it('redirects to /scan with URL when sessionStorage has pending URL')` -- set sessionStorage 'auditflow_pending_url' to 'https://test.com', submit login form, verify navigate called with '/scan?url=https%3A%2F%2Ftest.com'
   - `it('redirects to /dashboard when no pending URL')` -- clear sessionStorage, submit login form, verify navigate called with '/dashboard'
   - `it('clears sessionStorage after reading pending URL')` -- verify removeItem called with 'auditflow_pending_url'
   - `it('renders login form with email and password fields')` -- basic smoke test

2. **Fix `src/components/sidebar.test.tsx`** (3 failures):
   - Read the current test file and the current sidebar.tsx implementation
   - Update test assertions to match actual sidebar DOM structure (nav items, active state classes, link hrefs)
   - Fix active nav highlighting assertions to match current implementation
   - Use `data-testid` or role queries that match the real component

3. **Fix `src/layouts/DashboardLayout.test.tsx`** (3 failures):
   - Read current test file and DashboardLayout.tsx
   - Fix credit balance assertion: match actual rendered text/structure from useCredits
   - Fix signOut button assertion: match actual button text/role
   - Fix null credits handling assertion
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && grep -c "it(" src/pages/Login.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "auditflow_pending_url" src/pages/Login.test.tsx
    - grep -q "sessionStorage" src/pages/Login.test.tsx
    - grep -q "/scan" src/pages/Login.test.tsx
    - grep -q "/dashboard" src/pages/Login.test.tsx
    - grep -q "removeItem" src/pages/Login.test.tsx
    - test -f src/pages/Login.test.tsx
  </acceptance_criteria>
  <done>Login.test.tsx has 4 test cases for AUTH-01 sessionStorage flow. sidebar.test.tsx 3 failures fixed. DashboardLayout.test.tsx 3 failures fixed. Login tests will fail (red) until Plan 02-01 Task 3 implements the sessionStorage logic in Login.tsx.</done>
</task>

</tasks>

<verification>
- `grep -c "it(" src/pages/NewScan.test.tsx` -- at least 8 test cases
- `grep -c "it(" src/pages/AuditDetail.test.tsx` -- at least 11 test cases
- `grep -c "it(" src/pages/Login.test.tsx` -- at least 4 test cases
- `npx vitest run src/components/sidebar.test.tsx src/layouts/DashboardLayout.test.tsx` -- pre-existing failures fixed, these tests pass
</verification>

<success_criteria>
- NewScan.test.tsx covers SCAN-01 through SCAN-04 requirements
- AuditDetail.test.tsx covers AUDIT-01 through AUDIT-05 requirements
- Login.test.tsx covers AUTH-01 sessionStorage preservation
- sidebar.test.tsx and DashboardLayout.test.tsx pre-existing failures are fixed and pass
- Test files define the behavioral contract for Plans 02-01 and 02-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-scan-results-redesign/02-03-SUMMARY.md`
</output>
