---
phase: 02-scan-results-redesign
plan: 02
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - src/pages/AuditDetail.tsx
  - src/components/audit-detail/ScoreRing.tsx
  - src/components/audit-detail/ScanningView.tsx
  - src/components/audit-detail/AuditHeader.tsx
  - src/components/audit-detail/ViolationCard.tsx
  - src/components/audit-detail/ViolationList.tsx
  - src/components/skeletons/AuditDetailSkeleton.tsx
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05]

must_haves:
  truths:
    - "User sees circular WCAG score ring with green/yellow/red color coding"
    - "User sees animated scanning state with 3 step indicators while audit is scanning"
    - "User sees collapsible violation cards with Why This Matters and How to Fix sections"
    - "User can filter violations by severity using tab row with count badges"
    - "User sees Download PDF button that shows coming soon toast"
  artifacts:
    - path: "src/components/audit-detail/ViolationCard.tsx"
      provides: "Collapsible violation card with AI explanation and fix steps"
      exports: ["ViolationCard"]
    - path: "src/components/audit-detail/ViolationList.tsx"
      provides: "Tab-filtered violation list with All/Critical/Serious/Moderate/Minor"
      exports: ["ViolationList"]
    - path: "src/components/audit-detail/ScanningView.tsx"
      provides: "Animated 3-step scanning progress view"
      exports: ["ScanningView"]
  key_links:
    - from: "src/pages/AuditDetail.tsx"
      to: "useAudit"
      via: "polling hook for audit data"
      pattern: "useAudit"
    - from: "src/components/audit-detail/ViolationList.tsx"
      to: "Tabs"
      via: "shadcn Tabs for severity filter"
      pattern: "TabsList|TabsTrigger"
    - from: "src/components/audit-detail/AuditHeader.tsx"
      to: "sonner"
      via: "toast for PDF coming soon"
      pattern: "toast.*coming soon|toast.*PDF"
---

<objective>
Redesign AuditDetail.tsx and all audit-detail sub-components to match Figma: circular SVG score ring, animated scanning state with step indicators, collapsible violation cards with AI content, severity tab filter with count badges, and PDF download button with "coming soon" toast.

Purpose: Users get a rich, informative audit results page that clearly presents AI-generated fix instructions.
Output: Redesigned AuditDetail page with all sub-components updated.
</objective>

<execution_context>
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vlad_nemyrovskyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scan-results-redesign/02-CONTEXT.md
@.planning/phases/02-scan-results-redesign/02-RESEARCH.md

<interfaces>
From src/types/index.ts:
```typescript
export interface Violation {
  id: string
  violation_type: string
  impact: 'critical' | 'serious' | 'moderate' | 'minor'
  wcag_criterion: string
  description: string
  ai_explanation: string | null
  ai_fix_steps: string | null
  affected_elements: string[] | null
  estimated_fix_hours: number | null
  page_url: string
}

export interface Audit {
  id: string
  website_url: string
  status: 'pending' | 'scanning' | 'completed' | 'failed'
  wcag_score: number | null
  wcag_level: string | null
  total_violations: number | null
  critical_count: number | null
  serious_count: number | null
  moderate_count: number | null
  minor_count: number | null
  created_at: string
  completed_at: string | null
}
```

From src/hooks/useAudit.ts:
```typescript
export function useAudit(id: string | undefined): { audit: Audit | null; loading: boolean; error: string | null }
```

From src/lib/format.ts:
```typescript
export function getScoreColor(score: number): string  // "text-green-600" / "text-yellow-600" / "text-red-600"
export function getScoreBg(score: number): string      // "bg-green-50" / "bg-yellow-50" / "bg-red-50"
export function formatRelativeTime(dateString: string): string
```

From src/components/audit-detail/constants.ts:
```typescript
// Severity color mapping exists here -- reuse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ScoreRing, ScanningView, and AuditHeader sub-components</name>
  <files>src/components/audit-detail/ScoreRing.tsx, src/components/audit-detail/ScanningView.tsx, src/components/audit-detail/AuditHeader.tsx</files>
  <read_first>src/components/audit-detail/ScoreRing.tsx, src/components/audit-detail/ScanningView.tsx, src/components/audit-detail/AuditHeader.tsx, src/components/audit-detail/constants.ts</read_first>
  <action>
1. **Update `ScoreRing.tsx` (AUDIT-01):**
   - Keep existing SVG circle math (r=44, circ=2*PI*r, offset calculation)
   - Set stroke width to 8 (strokeWidth={8} on both background and progress circles)
   - Score number centered inside: `text-3xl font-bold` with color from score threshold
   - Below the score number (inside SVG or as sibling): WCAG level label (e.g., "WCAG AA") in `text-sm text-gray-500`
   - Colors: green (`#22c55e` / green-500) for score >= 80, yellow (`#eab308` / yellow-500) for score >= 60, red (`#ef4444` / red-500) for score < 60
   - Add CSS transition on stroke-dashoffset: `transition: stroke-dashoffset 1s ease-in-out` via style prop
   - SVG viewBox="0 0 100 100", width/height 160px

2. **Rewrite `ScanningView.tsx` (AUDIT-02):**
   - Animated scanning state with 3 step indicators matching NewScan pattern
   - Steps: "Crawling pages" -> "Running axe-core" -> "Generating AI fixes"
   - Each step: CheckCircle (done, green-500), Loader2 (active, animate-spin text-[#4F46E5]), empty circle (pending, border-2 border-gray-200)
   - Use Framer Motion motion.div for staggered entry (delay: i * 0.15)
   - Accept `currentStep` prop (number, 0-2). Parent controls step advancement.
   - Center the steps vertically with a "Scanning your website..." heading above
   - Add a subtle pulsing dot or progress indicator at top

3. **Update `AuditHeader.tsx` (AUDIT-05):**
   - Keep existing header layout (website URL, scan date, status)
   - PDF download button: top-right area, `<Button variant="outline">` with `<Download className="h-4 w-4 mr-2" />` icon and text "Download PDF"
   - On click: `toast('PDF report generation coming soon', { description: 'This feature will be available in a future update.' })` using sonner
   - Import `{ toast } from 'sonner'` and `{ Download } from 'lucide-react'`
   - Do NOT call `auditApi.downloadPdf()` -- that is Phase 4
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && npx tsc --noEmit 2>&1 | grep -E "ScoreRing|ScanningView|AuditHeader" | head -10; echo "---"; grep -c "toast" src/components/audit-detail/AuditHeader.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "strokeWidth.*8\|stroke-width.*8" src/components/audit-detail/ScoreRing.tsx
    - grep -q "WCAG" src/components/audit-detail/ScoreRing.tsx
    - grep -q "Crawling pages" src/components/audit-detail/ScanningView.tsx
    - grep -q "Running axe-core" src/components/audit-detail/ScanningView.tsx
    - grep -q "Generating AI fixes" src/components/audit-detail/ScanningView.tsx
    - grep -q "Loader2\|animate-spin" src/components/audit-detail/ScanningView.tsx
    - grep -q "coming soon\|PDF report generation" src/components/audit-detail/AuditHeader.tsx
    - grep -q "toast" src/components/audit-detail/AuditHeader.tsx
    - grep -q "Download" src/components/audit-detail/AuditHeader.tsx
  </acceptance_criteria>
  <done>ScoreRing shows circular SVG with score centered + WCAG level label + color coding. ScanningView shows 3 animated step indicators. AuditHeader has Download PDF button that shows sonner toast.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ViolationCard and ViolationList with collapsible cards and severity tabs</name>
  <files>src/components/audit-detail/ViolationCard.tsx, src/components/audit-detail/ViolationList.tsx</files>
  <read_first>src/components/audit-detail/ViolationCard.tsx, src/components/audit-detail/ViolationList.tsx, src/components/audit-detail/constants.ts, src/components/ui/tabs.tsx</read_first>
  <action>
1. **Rewrite `ViolationCard.tsx` (AUDIT-03):**
   - Collapsed by default: show violation_type, impact badge (colored dot + text), wcag_criterion, page_url
   - Click header row to toggle expanded state (useState boolean)
   - Expanded content with Framer Motion AnimatePresence:
     - "Why This Matters" section: `<div className="rounded-lg bg-gray-50 p-3">` with `<p className="text-xs font-medium text-gray-700 mb-1">Why This Matters</p>` and `<p className="text-sm text-gray-600">{violation.ai_explanation}</p>`
     - "How to Fix" section: same pattern with `How to Fix` heading and `violation.ai_fix_steps` (use `whitespace-pre-line` for multiline steps)
     - Only render each section if the AI field is not null
   - Impact color mapping: critical -> red (bg-red-100 text-red-800), serious -> orange (bg-orange-100 text-orange-800), moderate -> yellow (bg-yellow-100 text-yellow-800), minor -> blue (bg-blue-100 text-blue-800)
   - ChevronDown icon that rotates 180 degrees when expanded (`transition-transform rotate-180`)
   - Props: `{ violation: Violation }`

2. **Rewrite `ViolationList.tsx` (AUDIT-04):**
   - Use shadcn `<Tabs>` component for severity filter
   - Tab values: "all", "critical", "serious", "moderate", "minor"
   - Each TabsTrigger shows label + count badge: e.g., "Critical (3)"
   - Count badges: filter violations array by impact to get count per severity. "All" shows total count.
   - Filter logic: when tab value is "all", show all violations; otherwise filter by `violation.impact === tabValue`
   - Sort violations: critical first, then serious, moderate, minor (use constant severity order array)
   - Render filtered violations as `<ViolationCard>` list with `key={violation.id}`
   - Props: `{ violations: Violation[] }`
   - Import Tabs, TabsList, TabsTrigger from `@/components/ui/tabs`
   - Do NOT use TabsContent -- just filter the list based on active tab value via onValueChange + useState
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && npx vitest run src/pages/AuditDetail.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Why This Matters" src/components/audit-detail/ViolationCard.tsx
    - grep -q "How to Fix" src/components/audit-detail/ViolationCard.tsx
    - grep -q "AnimatePresence" src/components/audit-detail/ViolationCard.tsx
    - grep -q "ChevronDown" src/components/audit-detail/ViolationCard.tsx
    - grep -q "ai_explanation" src/components/audit-detail/ViolationCard.tsx
    - grep -q "ai_fix_steps" src/components/audit-detail/ViolationCard.tsx
    - grep -q "TabsList\|TabsTrigger" src/components/audit-detail/ViolationList.tsx
    - grep -q '"all"' src/components/audit-detail/ViolationList.tsx
    - grep -q '"critical"' src/components/audit-detail/ViolationList.tsx
  </acceptance_criteria>
  <done>ViolationCard collapses/expands with AI content sections. ViolationList has severity tab row with count badges filtering the violation card list. All AuditDetail tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire AuditDetail.tsx to use redesigned sub-components</name>
  <files>src/pages/AuditDetail.tsx, src/components/skeletons/AuditDetailSkeleton.tsx</files>
  <read_first>src/pages/AuditDetail.tsx, src/components/skeletons/AuditDetailSkeleton.tsx</read_first>
  <action>
1. **Rewrite `AuditDetail.tsx`:**
   - Use `useAudit(id)` hook for data + polling
   - Loading state: render `<AuditDetailSkeleton />`
   - Scanning state (`audit.status === 'scanning'`): render `<ScanningView currentStep={scanStep} />` with simulated step advancement (0 initially, 1 after 5s, 2 after 10s via useEffect + setTimeout)
   - Completed state layout:
     - Top: `<AuditHeader>` with audit metadata + PDF button
     - Below header: summary row with ScoreRing on left, stat cards on right (Total Violations, Critical, Serious, Moderate, Minor counts)
     - Below summary: `<ViolationList violations={audit.violations || []} />`
   - Failed state: error card with red background, "Scan failed" message, retry button
   - Clean layout: max-w-5xl mx-auto, consistent spacing (space-y-6)

2. **Update `AuditDetailSkeleton.tsx`:**
   - Match the new completed-state layout: skeleton ring (circle), skeleton stat cards (4 rectangles), skeleton tab row, skeleton violation cards (3-4 card shapes)
   - Use shadcn `<Skeleton>` component with appropriate widths/heights

3. **Step simulation for scanning state:**
   - `const [scanStep, setScanStep] = useState(0)`
   - `useEffect` when `audit?.status === 'scanning'`: set timeout for step 1 at 5000ms, step 2 at 10000ms
   - Clean up timeouts on unmount or status change
  </action>
  <verify>
    <automated>cd "/Users/vlad_nemyrovskyi/Desktop/Projects/Accessibility Audit generator" && npx vitest run src/pages/AuditDetail.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ScanningView" src/pages/AuditDetail.tsx
    - grep -q "ScoreRing" src/pages/AuditDetail.tsx
    - grep -q "ViolationList" src/pages/AuditDetail.tsx
    - grep -q "AuditHeader" src/pages/AuditDetail.tsx
    - grep -q "AuditDetailSkeleton" src/pages/AuditDetail.tsx
    - grep -q "scanStep\|setScanStep" src/pages/AuditDetail.tsx
    - grep -q "Skeleton" src/components/skeletons/AuditDetailSkeleton.tsx
  </acceptance_criteria>
  <done>AuditDetail renders skeleton (loading), ScanningView (scanning), or full results (completed) with ScoreRing, stats, and ViolationList. AuditDetailSkeleton matches new layout. All AuditDetail tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/pages/AuditDetail.test.tsx` -- all tests pass
- `grep -q "Why This Matters" src/components/audit-detail/ViolationCard.tsx` -- AI content sections present
- `grep -q "TabsList" src/components/audit-detail/ViolationList.tsx` -- severity tabs present
- `grep -q "toast" src/components/audit-detail/AuditHeader.tsx` -- PDF toast wired
</verification>

<success_criteria>
- ScoreRing shows circular SVG with score, WCAG level, color coding (green/yellow/red)
- ScanningView shows 3 animated step indicators
- ViolationCard collapses/expands to show AI explanation and fix steps
- ViolationList filters by severity tabs with count badges
- AuditHeader shows Download PDF button with "coming soon" toast
- AuditDetail orchestrates all sub-components with correct state handling
- All AuditDetail tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-scan-results-redesign/02-02-SUMMARY.md`
</output>
